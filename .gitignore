data/external/
# License-restricted utility data — MUST NOT be committed (redistribution prohibited).
# Owner-placed 関西TD 空容量マップ; see memory reference_utility_data_licensing.
/k_line.csv
# Pending connection-editor proposals awaiting adjudication (1-F), quarantined.
# These used to live in data/ where the builder auto-picks them up by naming
# convention (snapped_topology) — contaminating the canonical build (okinawa
# 99->105 buses) and failing 3 regression pins on any full-suite run. Moved to
# data/pending_1f/ (2026-07-14) so the canon stays supplement-free until the
# proper adoption workflow runs (move back + regen okinawa.json + pin update +
# before/after). See docs/reports/formal_review_2026-06-26.md (H4).
/data/pending_1f/
__pycache__/
*.egg-info/
build/
node_modules/
package-lock.json
.tmp_frames/

# Auto Claude data directory
.auto-claude/

# Claude Code session working dir (handover notes, etc.)
.claude/

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/

# macOS
.DS_Store

# Coverage / pytest artifacts (regenerable)
.coverage
.coverage.*
htmlcov/

# Powerflow regen staging / backups (scripts/regen_powerflow_snapped.sh)
docs/data/powerflow_snapped/
docs/data/powerflow.legacy_*/

# Pickled GridNetwork cache
*.pkl
data/cache/

# Unified grid DB — rebuildable via scripts/db/ingest.py (docs/DB_ARCHITECTURE.md).
# The tracked curation backup is data/db/enrichments.jsonl.
data/grid.db
data/grid.db-wal
data/grid.db-shm
data/db/export_preview/

# Marginalia annotation tool working dir
.marginalia/

# Stray LaTeX logs at repo root
/ieej.log
/texput.log

# IEEJ official class distribution (extracted archive; the ieej.cls actually
# used is tracked at papers/ieej.cls — md5-identical to ieej5.0/UTF/ieej.cls)
papers/ieej5.0/

# LaTeX build artifacts (papers/)
papers/*.aux
papers/*.bbl
papers/*.blg
papers/*.dvi
papers/*.fdb_latexmk
papers/*.fls
papers/*.lof
papers/*.lot
papers/*.out
papers/*.toc
papers/*.synctex.gz
papers/*.log
papers/*.pdf
papers/*.zip
papers/.DS_Store

# Generated analysis outputs (regenerable; key figures kept in docs/assets & papers/figs)
output/
data/audit/

# CIM/CGMES export — generated RDF/XML (regenerate via scripts/export_cim.py).
# Full national set is ~294 MB; only the okinawa sample + manifest are tracked,
# and the zipped release is published as a GitHub Release asset.
dist/cim/*_EQ.xml
dist/cim/*_GL.xml
!dist/cim/okinawa_EQ.xml
!dist/cim/okinawa_GL.xml
dist/cim/*.zip

# MATPOWER 正典エクスポート（18MB・scripts/export_national_matpower.py で再生成）
dist/matpower_canonical/

# SubSLD 実証ペア図ギャラリー（745MB / 7,239所ぶんのPNG。
# scripts/build_substation_structure.py --fig で再生成、実物は nas03 に退避）
data/subsld/

# Level-2 CGMES (EQ/TP/SSH/SV/GL per region; regenerate via export_cim_level2.py)
dist/cim_level2/*.xml
!dist/cim_level2/okinawa_L2_EQ.xml
!dist/cim_level2/okinawa_L2_TP.xml
!dist/cim_level2/okinawa_L2_SSH.xml
!dist/cim_level2/okinawa_L2_SV.xml
!dist/cim_level2/okinawa_L2_GL.xml
!dist/cim_level2/AllJapan_EQ_BD.xml
!dist/cim_level2/AllJapan_TP_BD.xml
dist/cim_level2/*.zip

# Intentionally-untracked working/scratch files (do NOT commit to this repo):
#  - REVIEW_FINDINGS.md : internal review worklist (self-declared untracked; still has open items)
#  - papers/main.md     : a DIFFERENT project (nationwide traffic simulation), not All-Japan-Grid
#  - papers/test_cls.tex: ieej.cls scratch test
REVIEW_FINDINGS.md
papers/main.md
papers/test_cls.tex
.playwright-mcp/

# MATPOWER national export (generated artifact — the script is the recipe)
dist/matpower_national/
data/osm_raw/
data/osm_raw_towers/
data/rebuild/
data/topology/*.jsonl
/cache/

# 構造DB(node-breaker): 地域ファイルはD層生成物(4秒で決定的に再生成可能)。
# 正典カタログ summary.json のみ追跡(生成器=scripts/build_structures_batch.py)
data/structures/*.json
!data/structures/summary.json

# 数値Ybus(D層生成物・全4島47秒で決定的に再生成)。品質記録 meta.json のみ追跡
dist/ybus/*.mat
dist/ybus/*.npz
dist/ybus/*_bus.csv
dist/ybus/*_branch.csv
dist/ybus/figs/

# 潮流アニメーション(生成物・~41MB。生成器=scripts/animate_powerflow_gif.py)
dist/pf_animation/

# データセット配布バンドル(生成物・Release経由で配布)。生成器=scripts/make_dataset_bundle.py
dist/bundle/

# チュートリアル実行成果物(利用者が生成)。テンプレ generators_template.xlsx は追跡する
dataset/02_uc_from_excel/uc_result.xlsx
dataset/02_uc_from_excel/uc_result.png
dataset/**/out/
dataset/**/*_res_bus.csv

# 感度行列(PTDF/LODF)の行列本体。密行列で west は 339MB になるため git には入れず
# scripts/sensitivity/build_sensitivity.py で再生成する。索引表(bus/branch csv)・
# meta.json・README は同梱する（再生成物の解釈と sha256 による同一性確認に要る）。
dist/sensitivity/*.npz
dist/sensitivity/*.mat

# 例題の実行結果（再生成できるので同梱しない）
dataset/03_sensitivity_screening/screening_*.csv
docs/data/built/all.json.pre_worklist.bak
docs/data/built/all.json.pre_v2.bak
docs/data/built/all.json.pre_route.bak
docs/data/built/all.json.pre_hygiene.bak
docs/data/built/all.json.pre_frag.bak
docs/data/built/all.json.pre_frag2.bak
data/osm_refresh/
~$*
docs/data/powerjp/
data/realtime/

# Office lock files
~$*
docs/data/agc/mm_traces_*.npz
docs/slides/ajg/.deck_backups/
