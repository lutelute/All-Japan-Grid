# 66kV級潮流の達成計画（自律ループ運転票）

**目標**(ユーザー指示 2026-06-11): 66kVまでの潮流計算が**意味を持って**回ること。DBとして整備。自律ループで前進。
**戦略注記**(ユーザー指示 2026-06-11): **154kVが肝**。OSM 1,301本=幹線(1,179本)超の規模で、66kV(5,067本)実測オフテイクが154/66変換点に集約される層。154の計器(現n=31/184回廊・p=0.71)と較正を最優先。
**ベースライン**(2026-06-11, フルモデル東京, n=384): trunk ρ=0.684 / 154kV ρ=0.069 / 66kV ρ=0.112 / 全体0.418
**計器改訂後ベースライン**(2026-06-11b, 変種マッチ, n=419): trunk 0.615(74) / 154 0.089(36, p=0.61) / 66 **0.145(307, p=0.011)** / 全体**0.473** — 66kVが初の統計的有意。以後の比較はこの物差しで
**達成条件(KPIゲート)**: 154kV ρ≥0.40・66kV ρ≥0.30・全体≥0.50（東京・内部）/ フルAC 10/10維持 / `--source db`同一性pin維持
**計測コマンド**: `python -m src.validation.external_tepco --flows --backbone 0 --json docs/reports/external_flows_tokyo_full_<date>.json`

## ループ運転規則
1. 下のチェックリストの**最初の未完了[ ]**を1つ実行 → 計測 → 台帳(IMPROVEMENT_LOG)1エントリ → ここのチェック更新 → commit+push → 次のwakeup予約(1800s)
2. ρ悪化(誤差帯超)はrevert検討と正直記録。コンテキスト低下時は引き継ぎ更新して安全終了
3. 重い計算はsgnbサーバー可。負の結果も[x]にして知見として記録
4. **系統図のLINE送信**(ユーザー指示06-11): モデルが変わる反復の後は `PYTHONPATH=. python scripts/render_grid_figure.py` で系統図を生成し `mcp__line-bridge__send_image` で都度送信(KPI注記つき)
5. **UCはYbus判定必須**(ユーザー指示06-11): この網でUCを解く際は必ず事前にYbus条件数ゲートを通す(島別cond推定→閾値超は正直にFAIL報告)。位置づけ=「Fableがどこまで良いものを作れるか」の挑戦 — 妥協せず計測で語る

## M1 計器（ほぼ済）
- [x] 3層クラス帯ρ(trunk/154/66)・フルモデル計測 (`2bc782f`+)
- [x] match_tepco(接続recall)の154/66拡張 → 帯別recall: trunk 54.9%/154 56.9%/66 **46.9%**(n=634) — 構造は~半分再現、ρ低迷は需要配置が主因と確証（㉜）
- [x] **154kV流れ標本の回収**(06-11㉝): 184回廊中**119本は幹線の重複掲載**→純154母集団=65本。OSM名の変種展開(`_model_name_keys`)でマッチ384→419・全体ρ0.418→**0.473**。154は31→36(p=0.61のまま)・**残り20本=都心154kV地中ケーブルのOSM未収載が計器上限** → ρ154の本命はM3。66kVは0.145(p=0.011)で初の有意
- [x] sweepレポートに3層ρを統合(㉞): `ajgrid validate --topology tokyo --solve --flows` → external_flowsレベル(render行+`--baseline`差分でρ退行検出)。CSV不在時は黙ってスキップ(CI安全)。0.473を再現確認

## M2 DB整備（較正の一級市民化）
- [x] `scripts/db/calibrate.py`(㉟): measured_line_stats(region,line_key,kv_floor,source PK + q50/p95/window) — 東京705回廊(trunk171/154帯59/66帯475, FY2024通年)をDB化。pipelineは境界重みをDB自動読み(無ければ従来)、validatorは`--flows --from-db`でCSV無しに**全8指標を完全再現**確認済

## M3 需要の地理配置（本丸 — 154kVが集約層: 66kV実測オフテイクは154/66変換点に集まりρ154に直結）
- [x] **実測需要地図の抽出**(㊱): 当初案の末端オフテイクは26件/2.1GWで偽陽性あり(新信濃FC・新木更津=片端計測の罠) → **県別CSVの母線(B)列が本命**と発見: 配電用変電所の母線通過=その需要。**busbar 1,201変電所+terminal 21=1,222件・q50計19.1GW**をDB(measured_bus_loads)へ。モデル名一致54%/9.5GW(配置可能性確認済)
- [x] `estimate_loads(measured_bus_loads=)`(㊲): 名前一致647バスへ実測p95を絶対値ピン(最低≥50kVバス選択・超過時スケール・residualは従来則)。pipelineはDB自動読み(`measured_loads="auto"`)、A/B用`--no-measured-loads`
- [x] 計測(㊲・**誤差帯の負の結果**): ρ 0.473→0.463 / 154 +0.008 / 66 −0.001・倍率1.12→1.09。**需要配置は現状の律速でない** — 診断: スコア対象66回廊476本の端点実測カバレッジ中央値0%(線路計測と母線計測は別集団) + 接続recall47%で半分は配線が違う → **律速はM4接続正確性**。物理的忠実性を根拠に採用(㉙と同基準)

## M4 下位網トポロジ忠実度
- [x] 66kV attachment recall改善(㊳): 未接続184件の59%は**1ホップ隣**(線名分節アーティファクト)→**グラフ隣接タイア**実装で recall 50.3→**56.3%**・66帯46.9→**53.1%**。真の分断は9+遠12件のみ=**配線はほぼ健全**。ρ66の仮説は M5の放射運用へ移行
- [x] 真の配線課題の実地検分(㊴): 20件中、誤マッチ(同名異線88-117km先)43件→**ホモニムガード**で正直分類(unattached 154へ浄化)。本物は (a)西厚木=孤立重複ノード(実ヤード=OSM岡田変電所・1.78km、キュレーション対象) (b)地中/山岳ケーブル断片(東新宿水道橋・箱根・新豊洲=OSM部分収載)。配線健全の結論は維持
- [x] 放射構造の検証(㊵): 66kV層は**既にほぼ樹状**(cycle rank=114/1,363変電所・放射端24%)→放射化の上振れは限定的。開示末端26件は片端計測で真値に不適。仲裁実験: **実測端点あり回廊ρ=0.189 vs なし0.110** — 需要配置は有効だが天井0.19=構造・定数が残る律速
- [x] 66kV層の品質KPI地域別台帳(㊶): `scripts/report_kv66_quality.py` → 東日本タグ率95-98%・tokyoカバー68.5%は最良 / **chubu 11.6%・kansai 17.2%・kyushu 16.3%=重度断片**。66流れ検証のtokyo限定は「最良ケースの計測」と明記

## M5 電気・運用（㊵の両輪戦略: 未計測需要の代理 + 構造/定数）
- [x] **66kV放射化プロキシ実験**(㊷・**負の結果**): インピーダンスMSTで114ループ開放→ρ66 0.144→0.138(誤差帯)・全体不変 → **不採用**。opt-inフラグ(`--radialize-66`)として保持(将来常開点情報が得られた時の道具)
- [x] **残余需要の地理代理**(㊸): e-Stat 1kmメッシュ34,823セル/49.2M人→Voronoi配分実装(`--spatial population`)。**154 +0.05の方向性**(0.097→0.148)だがtrunk −0.05・倍率悪化とトレードオフ・p未有意 → **opt-in採用・既定変更なし**。改良案=クラス限定傾斜/昼間人口
- [x] 66kVのR/X(㊹): 地域ヒューリスティック不要 — **OSMのpower=cableタグが直接証拠**(東京486 features)。XLPE変種(X≈架空1/3)をline_types.yamlに追加しbuilderへ配管、151枝/57kmケーブル化。ρは誤差帯(収載57kmでは解像不能)だが物理的忠実性で採用。基準計器=f
- [x] 154/66トランス妥当性(㊺): 全772台の負荷率分布を診断 — 154/66中央値15%/最大78%・系統的過負荷なし・vk標準値 → **歪み源ではない、変更不要**。M5完了

## M6 仕上げ
- [x] KPIゲート判定(㊻): **未達** — 154: 0.11/0.40・66: 0.14/0.30・全体0.46/0.50。READMEに到達点と**データ律速の天井**(OSM地中未収載㉝/開示の計測点分離㊲/常開点非公開㊷)を証拠つき記載。v1.5.0タグは保留(機能群はDB較正+計器で十分まとまっているが、ゲート未達のため誇大にならない範囲で次回判断)
- [x] 他地域展開メモ(㊻): READMEに記載 — 東日本(タグ95-98%)は構造的に準備済み・流れρ検証は東電開示のみ(他社の線別時系列は未発見)・西日本は断片解消が先決(カバー12-17%)

## M7 天井突破プログラム（ユーザー指示 06-11「技術的に天井を破るのがあなたの仕事・名前はそこまで重要でない」 — ループ再開）
- [x] **流量保存則による需要状態推定 v1**(㊽): 計測回廊で系統を切り負荷側部分木の需要合計=計測流量にスケール(`flow_calibration.py`・入れ子は内側優先・実測ピン保護・ホールドアウト計器`--corridor-calib`)。連鎖切断バグ修正後 40回廊/2.6GW較正。**train(較正済)ρ 0.145→0.227だがtest 0.087=負の転移 → 既定不採用**。限界: 名前なし85回廊に届かず・ループ上71回廊は部分木が定義できず・p95と断面の統計不整合
- [ ] **v2: PTDF最小二乗の需要状態推定** — DC感度行列(PTDF)で全計測回廊流量を**同時に**フィット(min ||PTDF·P − flow||² + λ||P−P_prior||²、P≥0・総量保存)。貪欲部分木の欠点(ループ不可・バランス破壊)を原理的に解消。train/testホールドアウトで汎化を判定
- [ ] v2の判定: test側ρ66が0.14(無較正)を有意に超えれば採用、超えなければ「流量からの需要逆推定の限界」として記録

## M8 Ybus出荷品質ゲート（改訂06-11: **UCは別開発** — 本プロジェクトの責務は「UCが安心して解ける網」の保証まで）
- [ ] **Ybus条件数ゲート**: pandapower内部のYbus(島別)から条件数を推定する`ybus_gate()`を実装(SuperLU condest系)。閾値超の島を名指しでFAIL報告し、sweep/export時の標準検査に組込み — west教訓(下位網変圧器の悪条件)の恒久計装化。別開発UCへの引き渡し条件として文書化

## M9 地形・道路に沿う接続推定（ユーザー着想06-11: 「66kVは道路に沿うことが多い・地形が一番合理的」）
- [ ] **事前検証実験**: 既知の66kV線(OSM正解)が実際どれだけ道路に沿うか定量化 — 1県分のOSM道路(primary/secondary/trunk)を取得し、66kV線の経路点が道路からXm以内にある割合 vs 154kV+の同割合を計測(=「66kVは道路沿い」priorの強度を数字に)
- [ ] **道路経路スコアの再接続**: 分断断片の端点ペア(≤数km)に対し、道路ネットワーク上の経路長/直線距離比でもっともらしさをスコア → 上位を synthetic線(prov=road_route)として接続し、recall/ρ/AC収束で判定。西日本の断片解消(chubuカバー11.6%等)が本命の適用先
- [ ] (発展) 地形コスト(DEM)版: 高圧クラスは最小地形コスト経路、66kVは道路コスト経路 — クラス別の経路モデル

---
## 終了記録 (2026-06-11) ※M7-M8追加により継続中
チェックリスト全15項目完了(負の結果4件を含む)。**KPIゲート未達=データ律速の天井を証拠つきで確定**が本プログラムの成果。再開条件: (a)OSM都心ケーブル収載の進展 (b)他社の線別開示発見 (c)常開点情報の入手 — いずれかで計器の天井が上がる。台帳㉛–㊼・基準計器=external_flows_tokyo_full_2026-06-11g.json(㊼=完了後のユーザー指摘による訂正: eponym連結44%・「別ヤード」主張撤回・配置タイア追加)
