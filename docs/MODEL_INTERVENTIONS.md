# モデル介入台帳 — 「繋がって/解けて見える」を作っている仮定の全リスト

- 制定: 2026-07-07 / オーナー指示:
  「専門知識がないと、つながったと信じ込んでしまう。盲信につながる修正をたくさん
  したので、これも技術的記録として残すべき」
- 発端: 幻tie事例（`reports/case_study_phantom_tie_2026-07-07.md`）—
  内部的に無矛盾なモデルは実在しない設備を平然と含み得ると実証された。
- **原則**: モデルを「解ける/繋がる/完全」に見せる介入はすべて、
  **①根拠 ②帳簿（どこに開示されるか） ③無効化手段** の3点セットでここに登録する。
  登録なき介入の追加は認めない（出典必須DBの「捏造REJECT」と同格の規約）。

## なぜこの台帳が必要か（盲信の構造）

本モデルの成果物（全国潮流の収束・整った系統図・tie表）は、以下の介入の**合成**である。
介入はそれぞれ正当化を持つが、合成結果は「実在の系統の観測」ではなく
「仮定を重ねた推定」である。専門知識のない読者は完成品の滑らかさから前者だと信じ込む
— 幻tieでは**作った我々自身すら**、突合で外部正解に当たるまで信じていた。
介入が増えるほど成果物は良く見え、盲信リスクは上がる。だから介入の全量を1枚で見えるようにする。

## 介入一覧（built/計算系譜・2026-07-07時点）

凡例 — 種別: 【構造】接続・トポロジを作る / 【値】パラメータを埋める / 【配分】需給を置く。
リスク: ★★★=実在しないものを実在すると誤認させ得る / ★★=量を歪める / ★=局所的。

| # | 介入 | 種別 | リスク | 実装 | 根拠 | 帳簿・開示 | 無効化 |
|---|---|---|---|---|---|---|---|
| 1 | **越境stitch**（同一座標~110m・同電圧階級の県境スライス連結） | 構造 | ★★ | `src/powerflow/connectivity.py` STITCH_CELL | 同一座標=同一物理点（証拠=座標一致） | conn meta `n_stitch` | 抽出単位で見る |
| 2 | **OCCTO ACタイの明示追加**（島内地域対の連系を枝として付与） | 構造 | ★ | `connectivity.py` tie_edges | OCCTO公表の実在連系 | `n_tie`・tie名 | — |
| 3 | **発電所の最近傍接続**（プラント→最寄り変電所バス ≤20km） | 構造 | ★★★ | `run_full_powerflow_from_db.attach_generators` | 「発電所はどこかに繋がっている」仮定。**実際の接続先の証拠なし** | なし（**要改善**: 距離分布の帳簿化候補） | — |
| 4 | **backbone断片復帰**（断片上のgen/loadを地理最寄りbackboneバスへ、最大57.7km） | 構造 | ★★★ | `uc_to_pf_built.build_backbone_net` | 実在電源は現実に繋がっている（=現実の回復）。ただし**接続先バスは推定** | ledger全件（moved/from_fragment/距離max/越境数） | `--model full` |
| 5 | **zone領土再属性**（座標→県ポリゴン→エリア・県近似） | 構造 | ★★ | `src/powerflow/region_attribution.py` | 県→供給区域の対応（公知）。**飛騨神岡・熊野等の県内乖離は未処理**・周波数跨ぎは禁止 | `region_reattribution`(changes/skipped_freq)・`region_src`退避 | `territory=False` |
| 6 | **周波数ガード**（50/60Hz跨ぎの再属性拒否=抽出元ラベル温存） | 構造 | ★★ | 同上 AREA_FREQ | 県近似がFC設備を壊す実害（新信濃） | `skipped_freq`（522件） | — |
| 7 | **発電所osm_id dedup**（重複コピーを領土地域優先で1回採用） | 構造 | ★★ | `attach_generators(territory=True)` | bbox重なり由来の重複（幻tie事例⑤） | 統合件数をstdout（**要改善**: JSON化） | `territory=False` |
| 8 | **島境界注入**（UC連系フローを境界設備バスへsgen注入） | 配分 | ★★ | `uc_to_pf_built` BOUNDARY_POINTS | 設備バスはOSM実在を名前解決。**分割比は定格固定・実運用配分ではない**。受け皿なき点は再配分（佐久間→新信濃0.83等） | `boundary_injection`/`boundary_mw`（時刻別） | フラグ省略（既定OFF） |
| 9 | **需要の合成配賦**（エリアピーク×電圧階級重みで変電所バスへ按分） | 配分 | ★★★ | `allocate_loads` | エリア計はOCCTO実績。**バス別の内訳は全くの合成** | なし（設定はdemand config） | — |
| 10 | **UC注入の容量比例配分**（燃料別合計MWを同燃料機へ容量比例で） | 配分 | ★★ | `src/uc/pf_injection.inject_dispatch` | 機別1:1対応が存在しない | clipped/unmatched（時刻別JSON・07-07から） | — |
| 11 | **成分別合成slack**（全連結成分にslack付与=断片も静かに給電される） | 配分 | ★★★ | `add_per_component_slacks` | 解けない成分を解くための数値的必須 | slack合計はKPIとして常時報告。**成分別内訳はdiagnose_slackで** | — |
| 12 | **容量欠損のデフォルト値**（fuel別: nuclear1000/coal600/gas400/…/fallback30MW） | 値 | ★★★ | `_DEFAULT_CAP`/`_CAP_FALLBACK` | 燃料別の典型値。**銘板の証拠なし**。okinawa事例で実害実証（47.3%の主因の片翼） | なし（**要改善**: 適用件数の帳簿化候補） | capacity_bridge適用で上書き |
| 13 | **capacity_bridge**（出典付き容量パッチ・大型機dedup・稼働炉リスト・zone_override） | 値 | ★★ | `src/uc/capacity_bridge.py` | data/reference YAML（出典note付き）・nuclear_status | 適用レポート（patched/dedup/retired/unmatched_patches） | `--bridge`省略（既定OFF） |

⚠ **介入の合成リスクの実例（2026-07-07）**: #13(bridge)+#8(境界注入)を**full全規模**へ合成すると
east のACが発散し、#17 の prune が網の9割を切断した見せかけ解に到達した（backbone では
同じ合成が健全: slack 3.06%）。単体で正当な介入も**合成先の構成で破綻し得る** —
新しい構成の初回実行では必ず給電率(served_frac)と損失の物理妥当性を確認すること。
| 14 | **線路の階級典型パラメータ**（R/X/C/max_i を電圧階級で一律） | 値 | ★★ | `get_line_parameters_safe`/line_types.yaml | 教科書典型値。導体・地中の個体差は無視 | なし（yamlが定義） | — |
| 15 | **変圧器の階級典型値+v4銘板**（既定=典型sn_mva、出典ありサイトのみ実銘板） | 値 | ★★ | `_TRAFO_PARAMS`+`load_nameplates` | 銘板=出典必須DB（existing 266）。**残り~1,500器は典型値** | `@nameplate`刻印・`n_trafo_nameplate` | `nameplates=None` |
| 16 | **不明電圧の66kVフォールバック**（kv≤0のノード） | 値 | ★★ | `build_island_net` | 送電網最下級として可解性維持 | なし | — |
| 17 | **AC prune ladder**（DC不可行枝を刈ってからAC・段階的閾値） | 構造 | ★★★ | `solve_island`/`uc_to_pf_built.solve_hour` | 発散する全規模ACを解くための数値手段。**刈られた枝の潮流は存在しない扱い**。⚠実害実証(2026-07-07): pruneが網の9割を切断した残片の収束を「AC成功」と報告する**見せかけAC解**(east full+bridge構成で served 6.2/57.4GW)→solve_hourに**給電率ガード**(served≥95%必須・未満は却下し次段へ)を追加 | prune段数・solved_mode・**served_load_mw/served_frac**(07-07から) | maxバス数設定 |
| 18 | **有界ACチェーン**（backbone系: 厳tol×100反復×3構成のみ・粘らない） | 値 | ★ | `uc_to_pf_built._BOUNDED_AC` | BLAS abort回避+緩tol解の物理的無意味さ | solver名がJSONに | — |
| 19 | **県別実需要シェア配分**（#9のzone内一様を、県別実需要シェア→県内電圧重みの2段に細分化） | 配分 | ★★ | `allocate_loads(pref_gwh=…)`+`src/powerflow/pref_demand.py` | 出典=電力調査統計3-(2)都道府県別電力需要実績FY2024年度計（資源エネ庁・URL/引用/checksum付きJSON=`data/reference/pref_demand_fy2024.json`）。**年間電力量シェア→ピークシェアの近似（県別負荷率差は無視）**・県がzoneを跨ぐ分（静岡富士川split・周波数ガード飛び地=長野の東電50Hz帯15.4%等）はsubノード数按分(share帳簿化)。動機=A案回帰の真因が#9の粗さと確定（`reports/a_plan_east_ac_regression_2026-07-08.md`）。⚠適用しても**east fullのACは回復しない**（誠実にdc_fallback。回復に見えた初版は飛び地バグのバラスト効果と判明し出荷前に棄却=同レポート§7） | `net._pref_demand_ledger`→uc_to_pf JSONの`pref_demand_ledger`（zone×県のn_bus/gwh/target_mw全件）・split_prefs | **既定ON**（2026-07-10既定化・`reports/default_on_decision_2026-07-10.md`）。`--no-pref-demand`=従来zone一様（回帰比較用） |
| 20 | **無効電力の局所補償**［2026-08-11 factor 0.6→**0.8** に再較正］（負荷バスへ容量性シャント=コンデンサバンクを付与し無効需要を局所供給） | 値 | ★★ | `--reactive-comp`→`src/powerflow/pipeline.add_reactive_compensation`（`allocate_loads`直後・slack付与前） | 実配電用変電所のコンデンサバンク/SVC（OSM欠落）のモデル化。動機=east full AC非収束の真因が**角度でなく無効電力/電圧崩壊**と診断確定（DC角度健全でprune0本・負荷無効19GVarが局所補償なしで高X66kV網を流れ電圧崩壊。`reports/east_network_reactive_2026-07-09.md`）。補償で誠実AC回復（給電98.2%・vm 98.4%が0.9-1.1pu帯）。**補償率0.6は一次資料アンカーの保守側**（`reports/reactive_comp_provenance_2026-07-10.md`: 四国EGC 2024実測換算で直近≈0.8・1990年代≈0.05のレンジ内。0.6=送電端力率0.991相当=2000年代中盤水準。単一公表値は存在せず換算には仮定4点・グラフ目視±10-15%）・0.8への引き上げは要再スイープの将来課題・vm外れ値41バス(0.66%)残存。**精緻化(07-10)**: 24h経路はシャントをbase断面で固定張りのため軽負荷時刻に過補償過電圧（t=3 vm2.99）→`--hourly-shunts`で時刻別地域負荷スケールに追従（コンデンサバンク投入/開放運用のモデル化・factor×Q_load(t)の本来意図）。効果: t=3 vm_max 2.99→1.77・vm_min 0.748→0.859・損失2623→2123MW（`reports/east_voltage_refinement_2026-07-10.md`）。既定OFF | JSONの`reactive_comp`（factor/n_shunt/q_comp_mvar）・meta`hourly_shunts` 。**2026-08-11 再較正 0.6→0.8**: 0.6 は太陽光既定 10MW（太陽光180GW）の頃の値で、介入#25 で 0.10MW に正した時点で合わなくなった。発電機は `max_q_mvar=0.5×容量` の無効電力源でもあるため、**水増し太陽光を消すと捏造された無効電力サポートも消える**（hokkaido vm<0.80 が 8→45バス）。0.8 は4島すべてで電圧・最大負荷率とも悪化させない（hokkaido 38→1 / okinawa 2→0 / east 0→0 / west 11→10）。⚠**捏造の付け替え**でもある（4島計 +9,000MVar 弱のコンデンサバンク増）— ただし後者は数えられ・開示され・切れる。経緯=`reports/reactive_factor_recalibration_2026-08-11.md`| **既定ON**（2026-07-10既定化・factor=config 0.6・`reports/default_on_decision_2026-07-10.md`）。`--no-reactive-comp`=補償なし（回帰比較用）。`--hourly-shunts`は別opt-in |
| 21 | **bbox二重抽出のdedup**（①重複ノード=同一座標6桁+kv を1バス ②重複エッジ=同一バス対+同一経路 を1本・parはmax保存） | 構造 | ★ | `build_island_net(dedup_nodes=True)`→`--dedup-nodes` | **除去であって接続追加でない**（座標/経路はOSM幾何由来ゆえ完全一致=同一物理点。bbox重なりで同一OSMオブジェクトが別regionに二重抽出された分）。検証（`reports/west_fragmentation_rootcause_2026-07-09.md`§5-7）: **飛騨変換所は同一osm_id=975217734が2抽出に存在**・ノード重複633の98.6%同名一致・エッジ重複1837の**99.6%が経路完全一致**・本物の複線(par>1単一エッジ8898)は不変・自己ループ0。効果=west断片化 2,531→544成分(east 532→312)・west線9793→8353(二重計上是正で損失+5.7%=より現実的)。⚠**断片化を直すがwest full ACの特効薬ではない**(dedup後もfactor≤0.6/CLI順序でdc_fallback)。周波数跨ぎ併合は起きない(同一島=同一周波数のみ処理) | bstatsの`n_dedup_merged`/`n_edge_dup_removed`→JSON`dedup_nodes`・Ybus meta.jsonの`dedup_nodes` | **既定ON**（2026-07-10既定化=オーナー承認・Ybus v5.0.0として正典再生成・`reports/default_on_decision_2026-07-10.md`）。`--no-dedup-nodes`=v4相当（回帰比較用・従来と完全一致の不変量維持） |

| 22 | **サイト内変圧器リンク**（同名変電所=電圧/_N複製サフィックス除去後の正規化名一致+空間クラスタ0.6km以内の異電圧階級バスを2巻線変圧器で連結） | 構造 | ★★ | `build_island_net(site_trafos=True)`→`--site-trafos` | 従来の変圧器は同一座標(_k5≈1m)のみ生成のため、同一サイトでも電圧階級ヤードが数十m離れると未連結（west T-gap 57%・東京城南チェーン低電圧の背景機構）。根拠=複数電圧階級を持つ同名変電所は変圧器で階級間を結ぶ実在構造そのもの。sub=1ノード限定・既存連結スキップ・銘板(@nameplate)適用は座標式と同一規則。**保守的R=0.6kmの実効は小**（east+6器/west+20器・west成分544→543・east外れ値不変=`reports/east_voltage_refinement_2026-07-10.md`）。**0.6-2.0kmのグレーゾーン54件**（加賀500/275・上ノ原500/275等の大物含む）は`probes/east_outliers_2026-07-10/sitetrafo_candidates.json`に候補化=**人間レビュー待ち・自動採用禁止** | bstats `n_site_trafo`・枝名`site_trafo_*` | `--site-trafos`省略（既定OFF） |
| 23 | **未供用線の正直化**（建設済み・供用開始前の送電線を出典必須リストで in_service=False 建て） | 構造 | ★ | `build_island_net(deenergize_unbuilt=True)`→`--deenergize-unbuilt`・正本=`data/reference/not_in_service_lines.json`（URL+逐語引用必須） | idle原子炉の正直化と同型=「物理資産は存在するが系統潮流を担っていない」の反映。初例=**大間幹線**（500kV・61km・大間原発は**運転開始 未定**=J-POWER一次）。モデル上通電すると無負荷EHV線61km(充電125MVar)のフェランチ効果で大間ポケットに vm1.33-1.75の過電圧を生んでいた。**効果: east過電圧バス12→0**（500kV 2+66kV 10全消滅・`reports/east_voltage_refinement_2026-07-10.md`）。供用開始確認で行を削除し再生成 | bstats `n_deenergized`・リストのquote/retrieved_at | `--deenergize-unbuilt`省略（既定OFF） |

| 24 | **発電機の接続電圧規則**（繋ぎ先を最寄りでなく受電容量／電圧階級で選ぶ）**［2026-08-09 既定ON＝`cap`］** | 構造 | ★★★ | `attach_generators(attach_mode=…)`→`--gen-attach {nearest,site,cap,kvfit}`・モデル既定は定数 `GEN_ATTACH_DEFAULT="cap"`（`uc_to_pf_built`・`sensitivity/*`・`diagnose_pf_frontier` も同定数を明示的に渡す）。**関数の引数既定は `nearest` のまま**＝what-if 群（`whatif_solar_default`/`whatif_stepdown`/`overload_vs_topology`/`repair_search` の base）は引数なし呼び出しで「旧既定＝最寄り」を比較基準として保つため。ここを動かすと公表済み診断の base が黙って cap に化ける | **#3 の精緻化であり、#3 の欠陥の是正**。最寄り規則は 66kV 変電所が桁違いに多いため接続先がほぼ 66kV になり、east は発電容量の **53.2%(99GW) が 66kV バス**、姉崎火力3,600MW・川崎火力3,420MW・横浜火力2,800MW まで 66kV 接続になっていた（実系統では 500/275kV）。判定基準は**モデル自身の導体定数**から測った階級の梯子（66kV 137MVA / 154kV 533 / 275kV 1,905 / 500kV 6,928MVA）だけで作り、**外部の接続電圧表は持ち込まない**（持ち込むと捏造）。効果=`reports/repair_search_2026-08-09.md`: east cap で過負荷 603→551、太陽光是正(#25)と併せて **603→303・最大 1,668%→823%**。**繋ぎ替えは外科的**（8,235機中167機・45,464MW、接続電圧が変わったのは72機のみ＝`reports/gen_attach_diff_2026-08-09.md` に名指しで開示）。⚠**接続先バスは依然として推定**であり実接続の証拠ではない（#3 と同じ★★★）。⚠**副作用（2026-08-10 発見）**: 受電容量で繋ぎ先を選ぶため**エリア境界を越えて繋ぎ替えうる**。west 8,775機中ゾーンが変わったのは2機だが、うち**舞鶴火力1,800MWが kansai→hokuriku** へ移り、`balance_by_zone` が座標zoneで容量を数えるため出力が1/3になる（大飯・高浜の計7,886MWは nearest 時点からの既存問題） | 実行ログ `介入#24 gen-attach=…: 繋ぎ替え N機/M MW`・`gen_attach_diff_*.md`（大型機の before/after を名指し）・**副作用**=`zone_attribution_dispatch_2026-08-10.md` | `--gen-attach nearest` |
| 25 | **燃料別既定容量による容量合成**（`capacity_mw` 欠落を燃料別の既定値で埋める）**［2026-08-10 太陽光を 10→0.10MW に是正］** | 値 | ★★★ | `_DEFAULT_CAP`／`attach_generators`→`--default-cap FUEL=MW` | **従来から存在したが未登録だった介入**（本表の規約違反を 2026-08-09 に是正）。モデル総容量 477GW の **48.3% が既定値による合成**（`reports/generation_fleet_audit_2026-08-09.md`）。とくに太陽光の既定 10MW は OSM 実容量の**中央値 0.10MW の 100倍**で、太陽光を 180GW＝実績ピーク 56.7GW の 318% に膨らませている。`balance_by_zone` はゾーン内を容量比例で配分するので、**既定値がそのまま発電の空間配分になる**（tokyo はゾーン発電の 52.6% が太陽光ノード）。⚠単独で 0.10MW に正すと east 最大負荷率は 1,668%→**3,371% と悪化**する — 膨らんだ太陽光が過負荷を隠していたため。**#24 と併せて初めて改善する**（cap+0.10MW で 823%）。交互作用の測定=`reports/repair_search_2026-08-09.md`。**2026-08-10 に 0.10MW へ是正（既定ON）** — 合成率 48.3%→20.1%。⚠**是正で新しい限界が現れた**: 太陽光は実績ピーク 56,684MW の **318%→20%** となり、今度は**下回る**（kyushu 2.6%・okinawa 0.6%）。OSM は小規模設備が大半で大規模事業所の`capacity_mw` が入っていないため、件数×中央値では実績に届かない。**既定値では解けない** — 出典付き容量充填（GEM 充填と同型）が要る。**その第一歩を 2026-08-10 に実施** — 出典付き容量 350 件（227GW）は 1-C GEM 充填で D層 `docs/data/plants_all.geojson` に入っていたが、潮流も CIM も R層 `data/*_plants.geojson` を読むため**誰も使っていなかった**（08-09 監査「`capacity_mw_sourced` を持つのは 0 件」）。**R層は書き換えず読む側がD層を引く**形で塞いだ（座標キーで 350/350 一致・重複0・出典値0は0のまま尊重＝大間原発）。合成率 **20.1%→14.0%**、kyushu **100%→33.3%**・okinawa **100%→3.9%** で「実容量ゼロ」の地域が消えた。無効化 `--no-sourced-capacity`。夕方ピーク断面の潮流への影響は小さいが、**昼間断面・RE 接続可能量の研究にはそのまま効く**（`generation_fleet_audit_2026-08-10.md`） | 実行ログ `介入#25 default-cap: FUEL 旧→新`・`出典付き容量: N件/M MW を出典値で置換`・合成割合は `generation_fleet_audit_*.json` の `synth_share`（**48.3%→20.1%→14.0%**・総容量 477,173→308,655→339,182MW） | `--default-cap solar=10.0` / `--no-sourced-capacity` |

| 27 | **187kV線路抵抗の実測較正**（`r_ohm_per_km` 0.038→0.060）**［既定OFF］** | 値 | ★ | `config/line_types.yaml` の `187.calibrated` + `get_line_parameters(..., calibrated=True)`（`kind="cable"` と同じ上書き機構）。**既定 False** なので明示的に要求した経路だけが使う | **推定ではなく外部実測との照合**。事業者公表の様式5インピーダンス（[[docs/reports/system_disclosure_survey_2026-08-11.md]]）の187kV **107本**で実測 X/R = **5.83**。標準表は 0.350/0.038 = **9.21** で実測の1.58倍。187kVは定義上そのまま**北海道・四国のみ**なのでこの107本は実質全数であり、**両社が独立に一致**（北海道65本 5.63 / 四国42本 6.15）＝標本の偏りではない。原因は `conductor: ACSR 330mm2 **x 2**`（2導体）の仮定が抵抗を半分に見積もっていたことと考えられ、逆算値 0.060 は単導体 ACSR 330mm2（154kV=0.050・132kV=0.045）と整合する。**x は据え置き**（迂回係数からの示唆は n=22 と弱く端点照合の誤差が混じるため、根拠の強い X/R のみ動かす） | `docs/reports/system_disclosure_survey_2026-08-11.md` §4.5。observed の原本は `data/external/system_disclosure/`（gitignore・再配布不可） | **既定OFF**。使うとき `calibrated=True` を明示。恒久的に消すなら yaml の `187.calibrated` ブロックを削除 |
| 26 | **発電機の計上エリアを operator で決める**（座標zoneでなく OSM の operator タグ→管内）**［2026-08-10 既定ON］** | 配分 | ★ | `attach_generators` が `zone_src` 列を付与 → `balance_by_zone(use_zone_src=True)`→`--gen-zone-by-operator`（**既定ON**・定数 `GEN_ZONE_BY_OPERATOR`） | **捏造ではなく、既にある出典を使う是正**。表は `src/uc/scenario.OPERATOR_REGION`（既存の単一出典）で、`capacity_bridge` が UC 経路向けに**同じ上書きを既に実装しており**その docstring に嶺南の事情が明記されている（「高浜/大飯/美浜は立地=福井県(hokuriku領土)だが関西電力の電源としてkansaiにディスパッチされる」）。銘板経路(`balance_by_zone`)だけがそれを消費していなかった非対称の解消。効果=`reports/zone_attribution_dispatch_2026-08-10.md`: 大飯 899→**2,668MW**・高浜 679→**2,013MW**・舞鶴 360→**1,068MW**（×3）。**集計の過負荷への影響は小さく方向も混在**（east 551→544・超過−5.5% / west 291→293・超過+6% / hokkaido・okinawa 不変）。⚠つまりこれは**個別発電所の出力の正しさ**のための修正であって、過負荷を減らす梃子ではない | 実行ログ `介入#26 gen-zone-by-operator: 計上エリアを変えた N機/M MW`（west 73機/17,541MW・east 18機/5,556MW） | `--no-gen-zone-by-operator` |

| 28 | **孤立変電所のTEPCO公表接続**（連結性監査の孤立変電所を、最寄りでなく**TEPCO系統情報公表の変電所×線路名**が明示する相手に繋ぐ）**［worklist・正典未適用］** | 構造 | ★★ | `scripts/reconcile_isolated_tepco.py`（潮流実績CSV列名から変電所×線路を抽出→孤立subの同一線相手が本系統subなら解決）→ `scripts/apply_tepco_connections.py`（正典を壊さずドライラン検証＋worklist化）。**正典 `docs/data/built/all.json` は不変**＝適用経路は未接続（実適用は接続編集ツール/ビルド手動接続入力で人間承認後）。連結性監査=`scripts/build_connectivity_audit.py`＋`classify_isolated_subs.py` | **`reconnector.py`（最寄りbus合成＝無理に繋ぐ）を避け、独立一次源で繋ぐ**。TEPCO PG は 66/154kV＋県別フローまで公表するので東京の局所系が解ける（他社は基幹系500/275kVのみ公表で解けない＝`docs/reports/isolated_multi_reconcile.json` の検証済み負の結果）。**東京の孤立A 77件中★10件が本系統変電所への接続線つきで解決**、うち都心275kV地中網7（高輪/永代橋/東新宿/新宿/墨東/城南/世田谷）＋東内幸町＋常盤台＋池上。例=城南→[新宿城南線]→江東275kV。⚠**接続の相手変電所は名前照合による推定**（TEPCOは「この線がこの変電所に付く」と言うが、モデルのどのバスかは名寄せ）。生CSV/潮流値は非収録（TEPCO転載禁止・`data/external`はgitignore・[[reference_utility_data_licensing]]） | `docs/reports/tepco_connection_worklist.json`（孤立→本系統の座標つき接続・エビデンス=線名）・ドライラン=**本系統外ノード 2130→2115（15減・10変電所＋連鎖5が合流）** | worklistを適用しない（既定＝正典不変）。適用しても worklist を外せば戻る |

| 29 | **実証接続 v2**（公表線路・分岐タップ・系統図判読・変圧器実証・同一敷地同定の5クラス83本＋region是正89ノード）**［worklist・正典適用待ち］** | 構造 | ★★ | `scripts/apply_disclosure_v2.py`（ドライラン既定・`--write`可逆・`--out`で正典を書かず影響測定・`--disable <class>`でクラス単位無効化） | ①`disclosure_line`(10): 東北NW潮流実績3年分(kikan+local01-07)の潮流正方向。下北半島チェーン(下北154/66・大畑・大湊・仲崎・佐井)＋中仙台・土樋 ②`disclosure_tap`(2): 公表from-toの「◯◯線分岐」経由（飯舘→原町・東通→大畑線分岐）③`disclosure_trafo`: 変圧器潮流実績CSV(一次/二次電圧・台帳201基=`normalized/tohoku_tr_registry.csv`) ④`same_site_identity`(70): **同名・同電圧・≤300mの本系統/孤立ペア＝跨region二重登録の同定**（中部↔東京境界の静岡/長野/山梨/群馬。topoRAGの跨region重複884組の監査該当分）。B判定(鉄道/自家用)と汎用名(tokyo_sub_N)は除外・300-800mの6件は人間確認送り。region是正89=同一敷地の本系統側region採用＋**青森箱**(下北半島のhokkaidoラベル20ノード=大間#28と同型のbbox混入)。⚠同名誤マッチ対策=region優先pick（茨城の大畑変電所への誤接続を実際に回避） | `docs/reports/disclosure_connection_worklist_v2.json`（クラス別・座標・根拠つき全82本＋region_fixes全89＋review6）・ドライラン=**本系統外1885→1799（86減・孤立変電所75合流）**※1885は連結性の島判定バグ修正(e54186f)後の基準 | worklist未適用が既定。適用後も `--revert`(bak)・`--disable`・git revert の3経路で戻る |

| 37 | **都心給電の必然接続(仮)**（上位変圧器を持たない負荷クラスタ≥100MWへ、最近傍≥275kVバスから(仮)変圧器を1台） | 構造 | ★★★ | `src/powerflow/pipeline.add_provisional_infeed`（`allocate_loads`+#20の後・slack前）→`--provisional-infeed` | **オーナー承認 2026-08-30**「良い。仮が事実でないかもしれないなら、それを明記しておけば正典として良い」。論法=推定母線と同じ「**負荷が現に供給されている以上、上位系からの給電経路の存在は電気的必然。存在のみ主張・経路とパラメータは(仮)明記**」。動機=west AC非収束の根因が大阪都心154kVクラスタの上位無接続(OSM欠測: 大阪の275kV地中網)と確定し(`reports/west_ac_probe2_2026-08-30.md`)、関西の開示系統図は実名匿名化(転載禁止)で#28型の出典回復が不可能。効果=**west backbone(≥154kV)のACがプロジェクト史上初めて成立**((仮)7件で mode=ac・served 96.5%・vm[0.941,1.037]・`reports/west_ac_infeed_probe_2026-08-30.md`)。⚠**(仮)は事実でない可能性がある** — 接続先(最近傍)は経路の仮定であり、実在の経路・電圧・容量は未確認。TEPCO型の公表(#28)や現地確認で実経路が判明したら**置換して(仮)を消す**前提の暫定。変圧器名に「(仮)都心給電#37 実経路未確認」を刻印。**第8波改良(2026-08-30)**: 負荷集計を「クラスタ除去で孤立する無電源成分の純負荷」込みに変更(大阪三国型=味生/西三国154の下に77kV 118MWがぶら下がる構図を閾値不変で検出、`reports/west_ac_wave7_2026-08-30.md`)+最近傍40km超は縫合せずcapped台帳のみ | 結果JSONの`provisional_infeed`(kv/load_mw/クラスタ名/接続先/距離/sn 全件)・変圧器名タグ | `--no-provisional-infeed`（回帰比較用） |
| 38 | **周波数跨ぎ再属性の精緻化**（周波数が県内で一意な県への抽出こぼれは、領土再属性(座標→県→エリア)を50/60Hz跨ぎでも実行） | 属性 | ★★ | `src/powerflow/region_attribution.reattribute_node_regions(freq_fix=True)`+`UNIFORM_FREQ_PREFS`→`--freq-fix-reattr`（両ドライバ既定ON）。`apply_node_hygiene.py`(#35)にも同ガードを追加 | 第6波診断(2026-08-30)の確定事実: westフルAC発散の震源=長野東信〜群馬の66/77kVポケットで、(i)群馬・埼玉・神奈川・山梨座標なのにregion=chubuの抽出こぼれ(嬬恋77kV群・神保原JR変電所・榛名275/500kV junction・鴨宮・リニア都留など)が従来の周波数跨ぎ全面ガードで**恒久残留**、(ii)#35(周波数ガード無し)がtokyo junction 8件をchubuへ逆流させていた(`reports/west_ac_onset_full_2026-08-30.json`+3方向裏取り)。ガードの本来の動機は**混在県**(長野=東信の一部50Hz、新潟=60Hz飛び地、静岡=富士川)の飛び地・越境幹線(安曇幹線等)の保護であり、一意周波数県(関東7都県+山梨=50Hz、愛知以西+北陸=60Hz)への是正まで塞ぐのは過剰。是正はドライランで275件(chubu→tokyo 266・tokyo→chubu 9)、長野の50Hz資産(tokyo→chubu 143件)はガード維持を確認。物理接続(OSM由来)には不変更 — regionラベルのみの現実回復で、#21(A案)の原則の延長 | 実行ログ「介入#38 跨ぎ是正」+戻り値`freq_fixed`(from->to件数)・`region_src`退避 | `--no-freq-fix-reattr`（回帰比較用） |
| 39 | **region_fixes の名前アサート適用**（disclosure v2 帳簿の region 是正を「IDの指す先の正規化名が帳簿nameと一致する場合のみ」適用。不一致=旧IDは name+from で唯一解決できれば救済、できなければ棄却。誤爆痕(名前不一致で region==to)は領土へrevert） | 属性 | ★★ | `scripts/apply_disclosure_v2.py`(適用点L527付近)。`--from-worklist --write` で正典 all.json を修復済(2026-08-30) | 第7波監査(`reports/west_ac_wave7_2026-08-30.md` §4)で確定した「旧ID適用事故」: 帳簿にノード再採番前の旧IDが重複収載され、ID直引きが旧IDを現在持つ無関係ノードを誤爆(例:「小海町」fixが佃野町変電所(神奈川)に命中)。修復実績= **revert 22 / rescue 11 / stale棄却 73**(全件実行ログ)。検証=west full AC・east AC・west backbone AC いずれも回帰なし(slack差±10MW以内) | 実行ログに全件([#39 revert]/[#39 rescue]行)・帳簿は不変(正本のまま)・`all.json.pre_v2.bak` で可逆 | 適用は帳簿再適用時のみ(通常ビルドは不変)。`--revert` でv2適用前へ |
| 40 | **県内按分の人口メッシュ傾斜**（e-Stat 1km国勢調査メッシュのVoronoi集計で電圧重みをbounded tilt 0.5+0.5·pop/mean） | 需要 | ★★ | `allocate_loads(pop_tilt=)`→`--pop-tilt`（両ドライバ・**既定OFF**） | 動機=県別×電圧階級の一様按分が過疎地に過大配分(江田島市4バス137.9MW vs 実勢≈30MW、`reports/west_ac_wave7_2026-08-30.md`§2)。**既定OFFの理由(2026-08-30検証行列)**: 手元メッシュ(data/external/estat)は**関東・中部タイルのみの部分被覆**(1次メッシュ5238-5540)で、①目的の江田島(被覆外)は無変化(34.5MW/バスのまま) ②被覆内だけ需要が歪み **east/westフルACがdc_fallbackに退行**。全国メッシュを整備してから再判定する(それまで実験フラグ)。west backboneは退行なし(ac維持) | 帳簿`net._pop_tilt_ledger`(n_bus_tilted・代理変数の注意書き)・実行ログ「介入#40 人口傾斜」 | 既定OFF(`--pop-tilt`で明示ON) |

（snapped系譜の旧介入 — 最近傍drop法・合成橋 — は built 系譜への移行で退役。
経緯は `reports/2026-06-10_fable5_osm_case_studies.md` と topology レポート群）

## 読み方 — 成果物を引用するときの注意

- **「AC収束」は #17 の刈り込み込み**の意味。「全規模AC」と書くときは prune ladder 経由を明示する。
- **slack%（需給整合KPI）は #9-#11 の合成の上の数字**。slackが小さい=現実に近い、ではなく
  「仮定群が内部整合した」まで。外部正解（OCCTO実績・公式潮流）との突合だけが実在性を担保する。
- **tie表は #5 の県近似の上の集計**。嶺南の自社幹線6.8GWが「hokuriku↔kansai」に載る等、
  連系線と域内幹線を区別できない（`reports/zone_reattribution_2026-07-07.md` §3）。
- **発電機の個別出力を引用しない**。#26 を OFF にしていると計上エリアが座標由来になり、嶺南原発群のような越境電源は**出力が 1/3 に出る**（大飯 4,494MW の機が 899MW）。
- **バス別・線別の潮流値を個別に引用しない**。#3/#9/#14 の合成であり、個別値に実在の裏付けはない。
  使ってよいのはエリア集計・傾向・相対比較まで（論文の限界節に明記のこと）。

## 保守規約

1. 新しい介入（接続を作る・値を埋める・配分を置く機構）を実装したら、**同一コミットで本台帳に行を追加**する。
2. 「帳簿・開示」欄が「なし」の介入は改善候補。新規実装では帳簿なし介入を認めない。
3. 介入を退役させたら行を削除せず「退役」と記す（幻tie事例の教訓: 過去の介入が過去の公表結果に残る）。

> **#28 追補(2026-08-15 非東京フォールバック)**: 他社は基幹系しか公表せず機械可読で解けないため、非東京の孤立A変電所はWikipedia/J-POWER(独立二次)へフォールバック(`docs/reports/isolated_fallback_wikipedia.json`)。名指し3件を出典付きverifiedで解決: **由良開閉所→紀北変換所**(阿南紀北直流幹線=DC連系・Wikipedia紀伊水道直流連系設備) / **上ノ国町→江差変電所**(Wikipedia上ノ国WF) / **大間町→上北**(大間幹線→むつ幹線・J-POWER aec.go.jp PDF)。⚠**モデル適用可否は繋ぎ先ノードの実在に依存**（繋ぎ先は変異名でモデルに実在＝江差変電所66kV/上北変電所500kV/紀北変換所）。**由良→紀北・上ノ国→江差は適用可**（TEPCO10件＋この2件で本系統外 2130→2112・18減・12孤立変電所が合流）。ただし紀北は非同期直流連系の変換所なのでAC枝でなくDC扱い要。**大間→上北は region是正が前提**＝大間町変電所が region 誤タグ（hokkaido・実体は青森=tohoku/east島）で島が違い枝が無視される。**region是正（大間町 hokkaido→tohoku・6ノード）を足すと合流する**（根拠=地理[下北半島=青森]・近隣佐井/大畑/東通がtohoku是正済[calibrate_islands]・J-POWER大間幹線→上北）。⚠calibrate_islands は大間をoperator無で未検出＝手動是正、[[project_agj_island_classify]] の region誤タグ系譜。**combined 最終=worklist13本＋region是正で本系統外 2130→2107（23減・13孤立変電所すべて合流）**。捏造回避=未解決8件は相手を空に。worklist=`docs/reports/tepco_connection_worklist.json`。

> **#28 追補2(2026-08-15 正典適用)**: オーナー実行の `apply_tepco_connections.py --write` で worklist13本＋大間region是正を **正典 `docs/data/built/all.json` に適用**（commit c4f0d23・本系統外 2130→2107・バックアップ=`all.json.pre_worklist.bak`[gitignore]・`--revert`可）。状態は「worklist・正典未適用」から**「適用済・可逆」**へ。

> **#29 経緯(2026-08-15 深夜)**: v2構築の過程で**連結性計算そのもののバグ**を発見・修正（`src/powerflow/connectivity.py` e54186f）。同一座標に別島ラベルの重複コピーがあると（東西境界203キー＋北海道/東北17キー実測）先勝ちの island_of が座標キーを奪い、もう片島の枝が黙って捨てられていた。**修正だけで本系統外 2107→1885（222ノード回復）**——下北半島はOSM線が存在するのにこのバグで孤立していた（=孤立監査705件の一部は検証器の人工物だった）。#29のドライラン基準値はこの修正後。v2の`--write`は分類器ブロックのためオーナー実行待ち: `python3 scripts/apply_disclosure_v2.py --write`

> **#29 追補(2026-08-15 早朝)**: ①東北の**様式5インピーダンス発見**（`impedance_2025_02.xlsx`・demandページ・基幹133線+変圧器72基。台帳の「東北未発見」を解消し正規化898線/190変圧器に拡大）— 既存解決の独立第二源の裏取りになった。②**局所系統構成図PDF判読**で `disclosure_map` クラスを追加: 佐井—161F線—大間町変電所_2（青森66kV図の座標照合済・沿岸66kVループの完成）。worklistは83本に。図で確定できなかった162C線の相手（岩屋 or 東通村・発電所番号310605の対応表要）と孤立側大間町変電所の素性は`map_reading_notes` に記録し未追加＝捏造回避。

> **#29 追補2(2026-08-15 05時台 正典適用)**: オーナー「正典も実行許可」により `apply_disclosure_v2.py --write` を実行。**本系統外1885→1799**・孤立変電所615→533・繋ぐべきA183→139（いずれも島判定バグ修正後の再計算基準）。バックアップ=`all.json.pre_v2.bak`(gitignore)・`--revert`/`--disable <class>`/git revertの3経路で可逆。監査(build_connectivity_audit)は--recompute不要になった（フラグが修正済み計算で焼き直された）。併せて classify の母集合を監査geojsonに統一し、判定上書き台帳 `config/isolated_verdict_overrides.yaml`（approved制・未承認提案6件=A降格候補）を新設。

> **#28/#29 追補3(2026-08-15 早朝・パイプライン組み込み)**: regenerate_all の先頭段 build_editor_data が **all.json を基底から再構築するため、in-place 適用した介入が regen のたびに消える**ことが実害として発覚（PFが8/11のpre-apply値に浮動小数点まで一致=入力が戻った兆候。事故記録=`reports/regen_wipes_interventions_2026-08-15.md`）。恒久修正: STEPS の build_editor_data 直後に **apply_disclosure_v1(--write) / apply_disclosure_v2(--from-worklist --write)** を組み込み（apply_capacity_sources と同じ「再構築後に必ず再適用」パターン・両者冪等）。**③無効化はパイプライン化以後「STEPSから外す or --disable」+ git が正**（bakは旧系譜のスナップショットになるため）。また #29 の本数は **83→76 が正**: 帳簿がバグ修正後の監査で再計算され、OSM線で既に繋がる下北チェーンの冗長エッジ7本が正しく脱落（並列回線二重計上の防止）。**実証接続の合計 = v1 13本 + v2 76本 = 89本**。

> **#29 追補4(2026-08-15〜16・判読キャンペーン+九州31地区プール)**: `disclosure_map` クラスを全国展開。①6地域並列判読(mapread_campaign_2026-08-15.md)と各社CSV/コード表アンカー解読で帳簿 89→153本・**供給ノード台帳 `config/disclosure_supplement_nodes.yaml` を新設**(判読で実在確定だがOSM抽出に無い局を注入・16局)。②九州31地区「予想潮流・空容量一覧表」を全面プール化(`scripts/pool_kyushu_kuyoryo.py`・831行)し 153→**157本**・供給ノード**17局**(大南追加)。犬飼(西大分—大南—犬飼)と湯山(=モデル「天瀬町赤岩」・OSM無名wayの住所合成名を九電名に同定)を解決。副産物として**住所合成名の分類artifact 2件**をoverrides提案(岱明町野口=き電20kV→B・敷川内町=ソーラー連系疑い→?)。詳細=`reports/kyushu_pool_campaign_2026-08-16.md`。帳簿・供給ノード・overridesの3台帳とも①根拠(src/evidence必須)②帳簿(git管理)③無効化(行削除 or --disable or approved:false)を満たす。

> **#29 追補5(2026-08-16・OSM実線形吸着)**: オーナー指摘「ちゃんと線があるものにおいては地形的に線を辿ってほしい。これは全部に言えること」を受け、`scripts/route_disclosure_edges.py` を新設しSTEPSに組込。実証コード90本を全数診断し、経路が**宙に浮いたOSM断片を主体とする(off-main比率≥0.7)12本のみ**=断片が公表線そのもの、と判定してコードを取付スタブ2本に置換(四国東部チェーン8本・泉北線・三田弥生が丘線・牛久線等。断片は実線形のままmain合流)。**別線迂回33本+OSM未収載45本は直線維持**(無い線形を捏造しない)。全記録=`reports/routed_disclosure_edges.json`(理由つき)。①根拠=off-main比率+迂回係数②帳簿=同json③無効化=STEPSから外す/BAK/git。連結性・KPIは不変(同じ接続を実経路で表現)・PF/Ybusは実線長ベースに改善。

> **#30(採用・既定ON 2026-08-17 オーナー承認): L_DB地点需要ピン留め配分** — `allocate_loads(point_demand=)`で観測地点(変圧器バンク実測mean)をピン留めしzone残余を従来配分(アンカー不変)。①根拠=7社バンク潮流実績(point_demand.csv 1,135行)の需要ビュー(二次kV≤22・逆潮除外) ②帳簿=`point_demand_ab_2026-08-17.json`(A/B: east/west/okinawaでAC収束・電圧・観測潮流整合すべて同等=**害なし・効果もまだ無し**) ③無効化=引数None(既定)。**保留理由=カバレッジ漏斗**: 1,135バンク→需要123バンク(70局・中間階級バンクは需要でないため正しく除外)→モデル名一致14局/286MW=全需要の0.3%。ボトルネックは住所合成名問題の再来で、ジオコード自動同定はauto 0/review 29/モデル不在26(`point_demand_alias_candidates_2026-08-17.json`)。**採用の前提=①review29局の判読審査 ②不在26局のノード化(需要既知の補完候補)**。**→ 2026-08-17 既定ON化**: マッチャー第2ラウンド(前方一致包含+電圧階級ガード+zoneキー付エイリアス, `point_demand_matcher_round2_2026-08-17.md`)でカバレッジ30バス/MW比68%に到達し前提①を充足。再A/B=`point_demand_ab_round2_2026-08-17.json`(east/west同等・沖縄はvm_min 0.750→0.758, 最大過負荷234.8→230.7%の微改善)。既定ON=`--point-demand`(無効化=`--no-point-demand`)。
> **#31(採用・既定ON 2026-08-17 オーナー承認「完全に2重計上なら1の推奨対応して」): 合成連系タイ・DC連系枝の非通電化** — オーナー指摘「四国関西の直線が長すぎる」を起点に監査(`tie_duplication_audit_2026-08-17.md`)。OCCTO定義から生成される合成直線タイ7本は、kv=0が両端の500kVを継承して**低インピーダンスの並列交流枝**としてPFに入っており、実連系線の実線形(関門連系線500kV・瀬戸大橋=菰池二丁目~福江町三丁目500kV・加賀~越前500kV・東近江~三重開閉所+三岐幹線・揖保~山崎開閉所+東岡山~西播)と**二重計上**だった。阿南紀北(DC)は本来ACループを切る設備なのに90km直線のAC枝を形成していた(`diagnose_zone_contamination.py`には「AC線ゼロが正」と設計意図が明記済み=実装と食い違い)。断面別のtie外し最短経路調査で6断面は完全二重を確認し `in_service=False` で建てる(連結性・表示・容量帳簿は保持)。**例外=東北東京間連系線は通電維持**(`KEEP_LIVE_TIES`): 相馬双葉幹線相当(南相馬~南いわき500kV)は実在するが南いわき開閉所地点(37.38,140.80)にbbox切れ端の**340m未縫合**(tokyo側の南いわき幹線115km・川内線27kmがdeg1で浮遊しタイが偶然縫合役)があり、実線形が未完のため。縫合後に除外へ揃える。あわせて阿南紀北直流幹線の実線形を取込(オーナー承認): OSMのunderwater cable(阿南~由良7点46km)を新規追加・由良~紀北はv1が108点実線形で再生成(`data/reference/anan_kihoku_route.json`)・由良開閉所500kVのdead-end解消。①根拠=BFS二重監査+断面別実経路+A/B(`tie_duplication_ab_2026-08-17.json`: 観測整合spearman/ratioとも不変・潮流は実線へ転流・関門−216→+383MW・東北東京タイは1,902MW運搬=切れ端の代表と確認) ②帳簿=n_tie_nisを実行ログに必ず出す+audit/ab両レポート ③無効化=`--synthetic-ties-live`(従来挙動)。

> **#32(採用・既定ON 2026-08-19 オーナー指示「3件は直しておいて」): 南福光BTBのAC素通し切断** — オーナー指示「大きな連系線や変換所は特に重点的に確認」の監査(`interconnector_converter_audit_2026-08-19.md`)で発見。中部北陸間は南福光連系所のBTB(back-to-back DC・非同期・定格300MW=OCCTO運用容量の中央値)による連系で交流の直通は実在しないが、モデルは南福光連系所バスで越美幹線(中部側)と加賀福光線・能越幹線(北陸側)が合流し、実績需要断面で575MW・UC断面で最大1,210MWがAC素通しになっていた(中央値運用容量の2〜4倍)。`build_island_net` にBTB_SPLITSを新設し、南福光連系所バスを分割して越美幹線の枝のみ「南福光連系所(中部側)」バスへ付け替える(変圧器・負荷・発電は北陸側に残置)。北陸の残る交流経路は関西側(越前嶺南線コリドー)=実系統と同じ。BTB融通のMW注入モデル化は#31の阿南紀北と同様に将来課題として明示。①根拠=OCCTO連系設備定義+運用容量正本(`data/interconnector_capacity_sources.jsonl` 南福光中央値300MW)+監査レポート ②帳簿=build統計 n_btb_split を実行ログに必ず出す ③無効化=`--no-btb-split`(回帰比較用)。

> **#33(採用 2026-08-19 オーナー指示「3件は直しておいて」): interconnections.yaml のOCCTO正本化** — 旧yamlはAI生成の近似定義で、(a) ic_005中部北陸=「加賀〜越前1,900MW AC」(両方とも北陸域内変電所。1,900は越前嶺南線の値と一致=隣の連系との混同) (b) 北陸関西間連系線(越前嶺南線)が丸ごと欠落 (c) ic_007関西四国=type:AC・「淡路島経由」(実際はDC・紀伊水道直流) (d) 容量が方向対称(実際は関門 順850/逆2,850など非対称) を含んでいた。全10連系をOCCTO系統情報公表(kohyo_04)の期間最大運用容量で再定義し、方向別容量 capacity_fwd_mw/capacity_rev_mw を導入(UC制約が方向別に適用・`Interconnection.cap_fwd/cap_rev`)。ic_005は南福光BTB定格300MW(中央値・fy2023r2のシナリオ側override 300と同判断・オーナー既往指摘2026-06-11「北陸に2,000MWもtielineは無い」)。ic_004三重東近江はOCCTO順方向=関西→中部(boundary.py検証済み対応表・台帳54)に従いfrom→to(中部→関西)=2,500。boundary.pyの_OCCTO_IC/MEASURED_UTILISATIONも追従(ic_005→南福光名・ic_010追加・除数再計算)。fy2023の「凍結」はこの正本化で解除(旧値は定義誤りと確定したため。テストも更新)。シナリオ側additions/overridesは冪等化(既存idはskip・capacity_mw単独overrideは方向別値をリセット)。**同時に発見したUC定式化バグも修正**: 地域収支が`>=`(不等式)で余剰の自由廃棄を許し、九州昼断面で純需要+輸出上限を5.7GW超える発電が「Optimal」になっていた(これがuc_to_pf注入を経てflow_mapのUC断面で関門5.8GW=運用容量2倍の潮流として可視化されていた)。等式+明示spill変数(ペナルティ・`UCResult.regional_spill_mw`帳簿)に修正し、`uc_to_pf_built`は揚水充電(負出力)を需要側に計上+spillを注入から控除=ゾーン純位置がUCスケジュールと厳密一致(修正後検証: 全地域残差0.0・全10連系×24時刻cap内・九州昼は揚水3.5GW充電+関門上限2,850輸出で整合)。①根拠=OCCTO正本jsonl(28レコード・原文quote)+UC再現実験 ②帳簿=yaml内provenance行+spill/charge_mwを時刻別レポートに出力 ③無効化=旧yaml保存(`data/reference/interconnections_legacy_2024.yaml`)+git revert。

> **#34(採用・既定=STEPS組込 2026-08-20 オーナー指示「そこまでやってみますか?お願いします」): OSM実線ブリッジの抽出回収(断片解消キャンペーン第一波)** — west_vr図検証で確定した本系統外断片(全国904成分/1,749ノード)に対し、`hunt_fragment_osm_bridges.py`が**OSMに実在する線(lines_all 40,087本)のうち断片ノードと本系統ノードの両方に物理接触(各≤80m・多くは0m=線上)しているのにbuilt枝になっていないもの**を検出し、実線形ごと回収した(接続の創作ではなく抽出漏れの回収)。**電圧整合ゲート**: 線kvとノードkv(判明分)が>25%乖離する候補は除外(併架・並走回廊の偶然接触を回収しない・500kV線×66kV断片等6件を実際に排除)。適用=106本 → **断片904→795成分(−109)・本系統ノード+240**(east−34成分/west−75成分)。ギャップ≤1km残は192→114。①根拠=OSM実線形そのもの+接触距離の帳簿(evidence.json・d_frag/d_main記録) ②帳簿=`docs/data/fragments/evidence.json`+各枝のdisclosure文とrecovery="osm_bridge"マーカー ③無効化=`all.json.pre_frag.bak`復元/recoveryマーカーで機械除去可。regen耐性=STEPSにfragment_recoveryとして組込(冪等)。same_site候補7件は適用せず提案のまま(承認待ち)。

> **#34追補(同日): 回収枝の経路切り出しバグ修正** — before/after図の目視検証で、回収枝pathが接触点間を切り出さず**OSM線全体を繋いでいた**(ループ状の過大経路・電気長も過大)ことを発見。`clip_path`(両接触点の最近傍頂点間スライス)を実装し、旧106本をrecoveryマーカーで機械除去→修正版で再適用(同106本・接続関係は不変・経路と電気長のみ是正)。**図による検証が実装バグの検出器として機能した実例**。

> **#34追補2(同日・第二波): OSM way連鎖の追跡回収103本** — オーナー観察「断片化箇所はOSMをよく見るとカクカクの線が見える=線は存在している。ただしkm離れた直線接続は良くない」を受け、`hunt_fragment_osm_chains.py`で**複数wayに分かれた実線形の連鎖**(断片→way1→…→wayN→本系統・継ぎ目各≤60m・最大6way・電圧整合ゲート)を辿って回収。89/103件は継ぎ目計ほぼ0m=OSMがwayを分割していただけ。直線ジャンプは構造的に不可能(辿るのは実線形のみ)。効果=断片795→691成分(−104)・累計(第一波込み)**904→691成分(−23.6%)・本系統+455ノード**。regen耐性はSTEPSのfragment_recoveryに連鎖版も追加要(→同スクリプトをSTEPS追記)。無効化=recovery="osm_chain"マーカー/all.json.pre_frag2.bak。
